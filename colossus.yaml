# colossus.yaml — generated by scaffolder, read by deploy agents.
# Do not delete. The deploy agents use this instead of framework auto-detection.
# {{IMAGE_NAME}} is a placeholder substituted by the deploy agent at build time.
#
# Single-container server-rendered app: Node 20 + Express + EJS + SQLite.
# The root Dockerfile builds the whole app; it listens on 8080 and prefixes all
# rendered links/forms/assets with BASE_PATH so it works behind the ingress at
# /{{IMAGE_NAME}}/*.
framework: node
projectName: e2e-lane-enterprise
dockerfile: Dockerfile
port: 8080
basePath: "/{{IMAGE_NAME}}"
