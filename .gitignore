# Dependencies — regenerated by `npm install` during the deploy build.
node_modules/

# SQLite database files (ephemeral, created at runtime).
data/
*.db
*.db-shm
*.db-wal
*.sqlite

# Logs
*.log
npm-debug.log*

# Editor / OS noise
.DS_Store
*.swp
