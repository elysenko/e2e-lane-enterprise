<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> · Habit Tracker</title>
  <link rel="stylesheet" href="<%= url('/static/styles.css') %>">
</head>
<body>
  <div class="app" data-testid="app-ready">
    <header class="site-header">
      <a class="brand" href="<%= url('/habits') %>">🌱 Habit Tracker</a>
      <nav class="site-nav">
        <a href="<%= url('/habits') %>">Habits</a>
        <a href="<%= url('/about') %>">About</a>
      </nav>
    </header>
    <main class="container">
